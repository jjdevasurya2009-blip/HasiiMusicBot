# Start a simple Python web server in the background to satisfy Render's port requirement
python3 -c "from flask import Flask; app = Flask(__name__); @app.route('/'); def h(): return 'OK'; app.run(host='0.0.0.0', port=int(os.environ.get('PORT', 8080)))" &

# Start your actual bot
python3 -m HasiiMusic
